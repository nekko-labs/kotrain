node_modules/
dist/
out/
build/
# but keep the desktop app's icon resources (electron-builder buildResources)
!apps/desktop/build/
!apps/desktop/build/**
*.log
.DS_Store
.env
.env.local
coverage/
*.tsbuildinfo
release/
cli-dist/
# runtime artifacts written by training/goal/design runs when this repo is
# itself used as a Kotrain workspace (dogfooding)
kotrain-training/
kotrain-goal/
kotrain-designs/
