node_modules/
dist/
out/
build/
# but keep the desktop app's icon resources (electron-builder buildResources)
!apps/desktop/build/
!apps/desktop/build/**
*.log
.DS_Store
.env
.env.local
coverage/
*.tsbuildinfo
release/
cli-dist/
# runtime artifacts written by training/goal/design runs when this repo is
# itself used as a Kotrain workspace (dogfooding). The nekkos-* names are from
# the interim rebrand and stay ignored so leftovers don't get committed.
kotrain-training/
kotrain-goal/
kotrain-designs/
nekkos-training/
nekkos-goal/
nekkos-designs/

# Local agent tooling (per-machine hooks and critique scratch)
.codex/
.impeccable/
