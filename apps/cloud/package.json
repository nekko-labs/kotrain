{
  "name": "@kotrain/cloud",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Nekko Cloud, multi-account hosted edition. Wraps the same host engine + UI per account behind accounts/auth/entitlements.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/static": "^10.1.0",
    "@fastify/websocket": "^11.3.0",
    "@kotrain/host": "*",
    "@kotrain/shared": "*",
    "fastify": "^5.10.0"
  }
}
