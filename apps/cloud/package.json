{
  "name": "@nekkos/cloud",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Nekkos Cloud, multi-account hosted edition. Wraps the same host engine + UI per account behind accounts/auth/entitlements.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/static": "^8.0.3",
    "@fastify/websocket": "^11.3.0",
    "@nekkos/host": "*",
    "@nekkos/shared": "*",
    "fastify": "^5.10.0"
  }
}
