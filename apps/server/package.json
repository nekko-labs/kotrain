{
  "name": "@nekko/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nekko Paw self-hosted web server — same engine + UI as the desktop app.",
  "main": "./dist/index.js",
  "bin": {
    "nekko-paw": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "bundle": "node scripts/bundle.mjs"
  },
  "dependencies": {
    "@fastify/static": "^8.0.3",
    "@fastify/websocket": "^11.0.1",
    "@nekko/host": "*",
    "@nekko/shared": "*",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
